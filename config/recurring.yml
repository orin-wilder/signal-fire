# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12
  prune_analytics_events:
    # The dashboard only queries the last 90 days; 180 keeps a safety margin.
    command: "AnalyticsEvent.where('occurred_at < ?', 180.days.ago).delete_all"
    schedule: every day at 4am
  weekly_digest:
    class: WeeklyDigestJob
    schedule: "0 13 * * 4"
    # Thursday 9am ET (UTC-4 summer) = 13:00 UTC
    # Winter (EST, UTC-5): change to "0 14 * * 4"
