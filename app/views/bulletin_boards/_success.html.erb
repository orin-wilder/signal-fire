<%# locals: totem %>
<h2 class="bb-success-title">Thanks — we'll take a look.</h2>
<p class="bb-success-body">If it's a fit, it'll show up on the board shortly. Nothing else needed from you.</p>
<%= link_to "Add another", bulletin_board_path(totem.slug), class: "bb-textbtn" %>
